#include <iostream>

using namespace std;

int main ()
{

    ios::sync_with_stdio(false);
    cin.tie(0);

    int n, m, i, j, k;

    cin >> n >> m;

    for (i=0; i<n; i++)
    {
        cout << 1;
    }

    cout << endl;

    for (i=0; i<n-1; i++)
    {
        cout << 8;
    }

    cout << 9 << endl;

   return 0;
}
